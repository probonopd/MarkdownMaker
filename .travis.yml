language: cpp
compiler: gcc
sudo: require
dist: trusty

before_install:
  - sudo add-apt-repository ppa:beineri/opt-qt593-trusty -y
  - sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
  - sudo apt-get update -qq

install:
  - sudo apt-get -y install qt59base qt59declarative qt59quickcontrols qt59quickcontrols2 gcc-5 g++-5 libgl1-mesa-dev
  - sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/c 60
  - source /opt/qt*/bin/qt*-env.sh
  - export CXX='g++-8'

script:
  # AxqLib
  - cd ..
  - git clone "https://github.com/mmertama/AxqLib"
  - cd AxqLib
  - qmake CONFIG+=release PREFIX=/usr
  - make -j$(nproc)
  - sudo make install
  - cd ..
  - cd MarkdownMaker
  # MarkdownMaker
  - qmake CONFIG+=release PREFIX=/usr
  - make -j$(nproc)
  - make INSTALL_ROOT=appdir -j$(nproc) install ; find appdir/
  - mkdir -p appdir/usr/share/applications ; cp markdownmaker.desktop appdir/usr/share/applications/
  - mkdir -p appdir/usr/share/icons/hicolor/246x256/apps ; cp markdownmaker.png appdir/usr/share/icons/hicolor/512x512/apps
  - wget -c -nv "https://github.com/probonopd/linuxdeployqt/releases/download/continuous/linuxdeployqt-continuous-x86_64.AppImage"
  - chmod a+x linuxdeployqt-continuous-x86_64.AppImage
  - unset QTDIR; unset QT_PLUGIN_PATH ; unset LD_LIBRARY_PATH
  - export VERSION=$(git rev-parse --short HEAD) # linuxdeployqt uses this for naming the file
  # Workaround for newer libstdc++ introduced by newer g++ from ppa
  - ./linuxdeployqt-continuous-x86_64.AppImage appdir/usr/share/applications/*.desktop -bundle-non-qt-libs
  - mkdir -p appdir/usr/optional/ ; wget -c https://github.com/darealshinji/AppImageKit-checkrt/releases/download/continuous/exec-x86_64.so -O ./appdir/usr/optional/exec.so
  - mkdir -p appdir/usr/optional/libstdc++/ ; cp /usr/lib/x86_64-linux-gnu/libstdc++.so.6 ./appdir/usr/optional/libstdc++/
  - ( cd appdir ; rm AppRun ; wget -c https://github.com/darealshinji/AppImageKit-checkrt/releases/download/continuous/AppRun-patched-x86_64 -O AppRun ; chmod a+x AppRun)
  - ./linuxdeployqt-continuous-x86_64.AppImage appdir/usr/share/applications/*.desktop -appimage

after_success:
  - find appdir -executable -type f -exec ldd {} \; | grep " => /usr" | cut -d " " -f 2-3 | sort | uniq
  - # curl --upload-file MarkdownMaker*.AppImage https://transfer.sh/MarkdownMaker-git.$(git rev-parse --short HEAD)-x86_64.AppImage
  - wget -c https://github.com/probonopd/uploadtool/raw/master/upload.sh
  - bash upload.sh MarkdownMaker*.AppImage*
  
branches:
  except:
    - # Do not build tags that we create when we upload to GitHub Releases
    - /^(?i:continuous)/
